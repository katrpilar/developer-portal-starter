{
  "title": "Locations map step",
  "description": "Locations map type step",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "stepTitle": {
      "title": "Title",
      "description": "The title of the step",
      "type": "string",
      "examples": [
        "Locations map step"
      ]
    },
    "stepDescription": {
      "title": "Description",
      "description": "The description of the step",
      "type": "string",
      "examples": [
        "This is a locations map step"
      ]
    },
    "stepType": {
      "title": "Type",
      "description": "The step type",
      "type": "string",
      "enum": [
        "locations-map"
      ]
    },
    "nextStep": {
      "title": "Next step",
      "description": "The next step the widget will go to after the user finishes with the current step",
      "type": "integer",
      "examples": [
        1,
        2,
        3
      ]
    },
    "filters": {
      "$ref": "_filters.json"
    }
  },
  "required": ["type"]
}